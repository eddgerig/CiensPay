services:
  backend:
    build: ./backend
    container_name: django_api
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
      - BROWSER=none

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: react_client
    depends_on:
      - backend

  nginx:
    image: nginx:alpine
    container_name: nginx_proxy
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - frontend
      - backend

# Comenta o elimina esta sección temporalmente:
# volumes:
#   frontend_node_modules:
#     driver: local
